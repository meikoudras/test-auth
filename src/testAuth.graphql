type AdditionPayload {
  sum: Int!
}

extend type Query {
  testAuth(a: Int! b: Int!): AdditionPayload
}

# type AdditionPayload {
#   sum: Int!
# }
#
# extend type Mutation {
#   add(a: Int! b: Int!): AdditionPayload
#}
